# The name people will type to reach Redline. Its DNS A record (and AAAA, if
# the server has IPv6) must point at this server before you start, or Caddy
# cannot obtain a certificate.
REDLINE_DOMAIN=redline.example.org
